node_modules/
dist/
.pnpm-store/
*.local
.env
.env.*
!.env.example
.DS_Store
coverage/
*.tsbuildinfo
.vite/
# Internal planning docs — private strategy stays local; HANDOFF.md is the committed source of truth
ROADMAP.md
ROADMAP2.md
DESIGN_FOLLOWUPS.md
# Local Claude Code launch config (machine-specific paths)
.claude/
# Local design-tool run log (per-machine rotation state, not project config)
.hallmark/
# Local scratch data (e.g. full raw HRSA/OPAIS dumps) — kept out of the repo
local-data/
# Generated knowledge graph (rebuildable from source; contains a large HTML bundle)
graphify-out/
